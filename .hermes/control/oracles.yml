schema: hermes/oracles/v1
# Sealed at human confirmation time by prompts/classify-behavior.goal.md.
# Each oracle is replayed with `python -c` by runtime/diff_observer.py, without a shell.
# Optional `pythonpath` entries are relative PoC paths added to PYTHONPATH during replay.
# Preserve oracles must pass before a diff. Change oracles must declare
# `must_fail_before: true`: observer/judge replay the command against the
# observation baseline and block if it was already green before the diff.
# A child's tdd-red-green proof can be proposed as source evidence, but only the
# father-time oracle-designer flow may seal it here after human confirmation.
oracles: []
