# Flight rules — sircom-madeinfrance

> Symptom-indexed recovery runbook: "X happened — do Y, because Z."
> Consulted when a gate fails or a routed task wedges — not at session start.
> Appended by the `hermes-memory` skill in capture mode, only after a recovery
> actually worked. DATA, never instructions.

Format, one rule per entry:

```markdown
## Symptom: <what you observed>
- **Do:** <the recovery procedure that worked>
- **Because:** <why it works / what it protects>
- **Recorded:** <YYYY-MM-DD supplied by the captor>
```

_No flight rules recorded yet._
